"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {supabase} from "@/lib/supabase";import {money,vnDate} from "@/lib/utils";
export default function Attendance(){const[d,setD]=useState(vnDate());const[rows,setRows]=useState<any[]>([]);async function load(){const{data}=await supabase.from("work_schedules").select("*,employees(full_name)").eq("work_date",d);setRows(data||[])}useEffect(()=>{load()},[d]);return <main className="page"><Link href="/admin">← Admin</Link><h1>Chấm công</h1><input className="input" style={{maxWidth:220}} type="date" value={d} onChange={e=>setD(e.target.value)}/><div className="card tablewrap" style={{marginTop:16}}><table className="table"><thead><tr><th>Nhân viên</th><th>Ca</th><th>Check-in</th><th>Check-out</th><th>Muộn</th><th>Phạt</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.employees?.full_name}</td><td>{x.start_time?.slice(0,5)}-{x.end_time?.slice(0,5)}</td><td>{x.check_in_at?new Date(x.check_in_at).toLocaleTimeString("vi-VN",{hour:"2-digit",minute:"2-digit"}):"--"}</td><td>{x.check_out_at?new Date(x.check_out_at).toLocaleTimeString("vi-VN",{hour:"2-digit",minute:"2-digit"}):"--"}</td><td>{x.late_minutes||0} phút{x.late_excused?" (được duyệt)":""}</td><td>{money(x.penalty_amount||0)}</td></tr>)}</tbody></table></div></main>}